# -*- coding: utf-8 -*-
"""Текстовые разделы пояснительной записки (стиль СибГУТИ)."""

CHAPTER_1: list[tuple[str, str]] = [
    (
        "1.1 Пиксельная графика и спрайты в видеоиграх",
        """Спрайт — это двумерное растровое изображение, которое отображается в игре поверх фона и перемещается независимо от него. В эпоху домашних консолей, например Nintendo Entertainment System (NES), разработчики работали с жёсткими ограничениями: палитра из нескольких десятков цветов, размер спрайта 8×8 или 8×16 пикселей, не более 64 объектов на экране одновременно. Именно эти ограничения сформировали узнаваемый стиль пиксель-арта — чёткие контуры, плоские цвета, минимум деталей.

Сегодня пиксельная графика переживает второе рождение. Игры Shovel Knight, Celeste, Stardew Valley и Hollow Knight показывают, что стиль востребован не только из ностальгии, но и как самостоятельная художественная форма. Современный neo-pixel art создаётся в разрешении 64×64, 128×128 и выше, однако сохраняет главное свойство — каждый пиксель осознан, границы не размываются антиалиасингом.

Для независимого разработчика создание спрайтов персонажей остаётся одним из самых трудоёмких этапов. Отрисовка одного персонажа вручную занимает от нескольких часов до целого рабочего дня, а при отсутствии профильного художника этот этап становится «узким местом» проекта. В связи с этим всё больше внимания уделяется методам автоматической генерации графики с помощью нейросетей.""",
    ),
    (
        "1.2 Обзор существующих решений",
        """Для систематизации обзора аналоги разделены на три группы: графические редакторы, локальные ML-инструменты и облачные сервисы.

К первой группе относятся Aseprite, Pyxel Edit и GraphicsGale. Aseprite является стандартом среди pixel artists: поддерживает послойную работу, палитры, onion skinning и экспорт sprite sheets. Эти инструменты не генерируют контент автоматически, но задают планку качества, к которой должна стремиться автоматическая генерация.

Ко второй группе относятся Automatic1111 WebUI, ComfyUI и Fooocus — оболочки над Stable Diffusion для локального inference. Automatic1111 поддерживает загрузку LoRA, ControlNet и пакетную генерацию, однако интерфейс ориентирован на опытных пользователей, не содержит специализации под игровые спрайты и не предоставляет встроенного сравнения нескольких моделей в одном окне.

К третьей группе относятся Midjourney, DALL·E 3 и Adobe Firefly. Они работают через облако, не позволяют дообучать модель на собственном датасете и не подходят для проектов, где важна конфиденциальность исходных материалов.

Сравнительные характеристики основных аналогов приведены в таблице 1.1.

Таблица 1.1 — Сравнение аналогов
Критерий | Aseprite | Automatic1111 | Midjourney | Разрабатываемая система
Автогенерация | − | + | + | +
Специализация под спрайты | + | − | − | +
Сравнение моделей | − | − | − | +
Веб-интерфейс | − | − | + | +
Локальное развёртывание | + | + | − | +
Собственный LoRA | − | + | − | +

Как видно из таблицы, разрабатываемая система занимает нишу специализированного веб-приложения для генерации игровых спрайтов с поддержкой собственного LoRA-адаптера. Переключение между тремя режимами inference реализовано как дополнительная функция для демонстрации эффекта дообучения, а не как самостоятельная цель работы.""",
    ),
    (
        "1.3 Выбор технологии Stable Diffusion и LoRA",
        """Среди генеративных моделей наибольшее распространение получили диффузионные модели — класс нейросетей, которые обучаются постепенно удалять шум из изображения. Stable Diffusion v1.5, выпущенная в 2022 году, стала одной из первых полноценно открытых моделей такого класса: веса, исходный код и инструменты доступны без коммерческих ограничений на базовое использование.

Прямое применение Stable Diffusion к синтезу пиксельных спрайтов без дополнительной настройки даёт неудовлетворительный результат: модель обучена на фотографиях и иллюстрациях, поэтому на выходе чаще получаются сглаженные градиенты и реалистичное освещение, что противоречит эстетике 8-bit графики.

Для адаптации большой предобученной модели к узкой предметной области применяют метод LoRA (Low-Rank Adaptation). LoRA добавляет к замороженным весам U-Net небольшие обучаемые матрицы, что требует на порядки меньше видеопамяти и времени, чем полное дообучение всей модели. Файл LoRA-адаптера занимает около 36 МБ, тогда как полная модель — около 4 ГБ.

Выбор Stable Diffusion v1.5 (а не SDXL или SD 2.x) обусловлен минимальными требованиями к VRAM (8 ГБ достаточно для обучения и inference), наибольшим количеством pixel-art LoRA в формате kohya и полной совместимостью с библиотекой diffusers.""",
    ),
    (
        "1.4 Постановка задачи и требования к системе",
        """На основании проведённого анализа сформулирована задача: разработать веб-систему генерации 8-bit спрайтов персонажей по текстовому описанию с поддержкой трёх режимов inference (базовая SD v1.5, публичная Pixel Art LoRA, собственная LoRA), обучить custom LoRA на датасете из не менее 500 пар «изображение — описание» и экспериментально сравнить качество генерации.

Функциональные требования:
1) ввод текстового промпта пользователем через веб-интерфейс;
2) выбор модели: base / public / custom;
3) настройка числа шагов денойзинга (10–50) и выходного размера (80 или 128 px);
4) отображение сгенерированного PNG и возможность скачивания;
5) хранение и просмотр истории последних 20 генераций;
6) REST API для программного доступа (/generate, /history, /health).

Нефункциональные требования:
1) inference на GPU NVIDIA с CUDA;
2) переключение LoRA без перезагрузки базовой модели;
3) модульная структура репозитория: backend, frontend, dataset, research.

Критерий успешности: пользователь через веб-интерфейс получает спрайт персонажа в пиксельном стиле; LoRA-адаптер обучен на датасете из 507 пар; все функциональные требования FR-1 – FR-6 выполнены.""",
    ),
]

CHAPTER_2: list[tuple[str, str]] = [
    (
        "2.1 Язык Python и фреймворк FastAPI",
        """Python был выбран в качестве основного языка для серверной части и ML-компонентов. Это обусловлено наличием библиотек PyTorch, diffusers и transformers, а также широкой поддержкой инструментов для обучения LoRA.

FastAPI — современный асинхронный веб-фреймворк для Python, построенный на Starlette и Pydantic. Он автоматически генерирует документацию OpenAPI (/docs), выполняет валидацию входных данных и обеспечивает высокую производительность.

Плюсы:
− простота разработки REST API;
− автоматическая валидация через Pydantic;
− встроенная документация Swagger;
− поддержка асинхронных эндпоинтов.

Минусы:
− синхронные GPU-операции блокируют event loop при отсутствии run_in_executor;
− для production с высокой нагрузкой требуется очередь задач.

Аналогами FastAPI являются Flask и Django REST Framework. Flask проще, но не имеет встроенной валидации и async-поддержки. Django REST Framework мощнее для CRUD-приложений с базой данных, но избыточен для данного проекта, где основная нагрузка — inference нейросети.""",
    ),
    (
        "2.2 Библиотека React и сборщик Vite",
        """React — JavaScript-библиотека для построения пользовательских интерфейсов на основе компонентов и реактивного состояния. В проекте используется React 19.

Vite — современный сборщик и dev-сервер, обеспечивающий быструю пересборку при разработке и оптимизированный production bundle.

Плюсы React:
− компонентная архитектура;
− большое сообщество и экосистема;
− простое управление состоянием через useState/useEffect.

Минусы React:
− для простого UI из нескольких форм может показаться избыточным;
− требует Node.js для сборки.

Плюсы Vite:
− быстрый hot module replacement;
− простая конфигурация proxy для dev-режима.

Аналогами являются Vue.js и Svelte. Vue проще для начинающих, Svelte компилирует компоненты в vanilla JS. React выбран как наиболее распространённый фреймворк с обширной документацией.""",
    ),
    (
        "2.3 PyTorch и библиотека diffusers",
        """PyTorch — фреймворк глубокого обучения от Meta, широко используемый для исследований и production ML. Поддерживает CUDA для ускорения вычислений на GPU NVIDIA.

Библиотека diffusers от Hugging Face предоставляет унифицированный API для работы с диффузионными моделями. StableDiffusionPipeline инкапсулирует загрузку модели, text encoder CLIP, VAE и U-Net, а также процесс inference.

Плюсы PyTorch:
− динамический граф вычислений;
− обширная экосystem (PEFT, bitsandbytes);
− поддержка mixed precision (fp16).

Минусы:
− высокое потребление VRAM (4+ ГБ для SD v1.5);
− зависимость от версии CUDA и драйверов NVIDIA.

Плюсы diffusers:
− единый API для загрузки и inference;
− поддержка load_lora_weights / unload_lora_weights;
− совместимость с safetensors.

Аналогом является оригинальная реализация Stable Diffusion от Stability AI, однако diffusers проще интегрировать в веб-приложение.""",
    ),
    (
        "2.4 Kohya_ss для обучения LoRA",
        """Kohya_ss (sd-scripts) — стандартный инструмент community для fine-tuning диффузионных моделей методом LoRA. Поддерживает SD 1.x, SDXL, настройку rank, alpha, learning rate, mixed precision и gradient checkpointing.

Плюсы:
− проверенный community-инструмент с большим количеством примеров;
− поддержка AdamW8bit для экономии VRAM;
− сохранение чекпоинтов по эпохам;
− совместимость формата safetensors с diffusers.

Минусы:
− интерфейс командной строки / GUI требует изучения параметров;
− нет встроенного val_loss на отложенной выборке (реализовано отдельным скриптом research/train_lora.py).

Аналогом является встроенное обучение LoRA в Automatic1111, однако kohya_ss предоставляет больше контроля над гиперпараметрами и является de facto стандартом для stylistic LoRA на датасетах 300–1000 изображений.""",
    ),
    (
        "2.5 Stable Diffusion v1.5 и метод LoRA",
        """Stable Diffusion v1.5 — latent diffusion model, работающая в сжатом латентном пространстве 64×64×4 вместо пространства пикселей 512×512×3. Это снижает вычислительную сложность примерно в 8 раз и делает возможным inference на GPU потребительского класса.

Архитектура включает три компонента:
1) VAE (Variational Autoencoder) — сжимает и восстанавливает изображение;
2) U-Net — ядро денойзинга, ~860 млн параметров;
3) CLIP Text Encoder — преобразует текстовый промпт в embeddings для cross-attention U-Net.

Метод LoRA добавляет к замороженным весам слоёв attention (to_q, to_k, to_v, to_out) низкоранговые матрицы. В данной работе использованы rank=32 и alpha=16, что соответствует рекомендациям community для style LoRA на датасетах 200–500 изображений.

На inference LoRA подключается через pipe.load_lora_weights() без перезагрузки базовой модели, что занимает 1–2 секунды вместо ~30 секунд полной загрузки pipeline.""",
    ),
]

CHAPTER_3: list[tuple[str, str]] = [
    (
        "3.1 Формирование датасета",
        """Датасет собран из открытых ресурсов, распространяющих asset packs под лицензиями, допускающими использование в проектах: itch.io, OpenGameArt.org и Kenney.nl.

Исходные файлы размещены в dataset/raw/ (PNG различных размеров, часто с прозрачностью). После предобработки в dataset/images/ получено 507 пар «изображение + текстовое описание».

Состав датасета по классам:
− гуманоиды (воины, маги, разбойники): ~35%;
− нежить (скелеты, призраки): ~18%;
− демоны и монстры: ~22%;
− животные и звери: ~12%;
− боссы и крупные существа: ~8%;
− прочие NPC и иконки: ~5%.

Каждому изображению соответствует caption на английском языке длиной 1–3 предложения с описанием типа существа, цветовой палитры, оружия и стилевых тегов: «8-bit pixel art character sprite», «NES style», «simple flat colors», «white background».

Пример caption (файл 0500_Imp3_Attack_without_shadow.txt):
«8-bit pixel art character sprite, red demonic imp with yellow glowing eyes, two large dark curved horns… NES style, retro game, simple flat colors, white background».""",
    ),
    (
        "3.2 Предобработка изображений",
        """Скрипт dataset/prepare_dataset.py реализует pipeline предобработки:

1) чтение PNG из каталога raw/;
2) фильтрация файлов с минимальной стороной менее 8 px;
3) обрезка до квадрата (center crop);
4) замена прозрачного фона на белый RGB(255,255,255);
5) масштабирование до 512×512 методом NEAREST (ближайший сосед);
6) сохранение в images/ с именем {index:04d}_{original_stem}.png.

Метод NEAREST выбран вместо билинейной интерполяции, поскольку последняя создаёт промежуточные цвета на границах пикселей и размывает контуры — главное свойство пиксель-арта.

Разрешение 512×512 соответствует native resolution Stable Diffusion v1.5. Белый фон обеспечивает согласованность между обучением и inference.

Для каждого PNG создан .txt файл с caption (507 файлов). Описания составлялись вручную по шаблону, а не генерировались автоматически, чтобы избежать ошибочных деталей.""",
    ),
    (
        "3.3 Обучение LoRA-адаптера",
        """Production LoRA custom_8bit_v2.safetensors обучена через kohya_ss (sd-scripts). Ключевые гиперпараметры приведены в таблице 3.1.

Параметры обучения:
− базовая модель: runwayml/stable-diffusion-v1-5;
− LoRA rank (network_dim): 32;
− LoRA alpha: 16;
− число эпох: 15;
− num_repeats: 5;
− learning rate: 1×10⁻⁴;
− оптимизатор: AdamW8bit;
− точность: fp16;
− GPU: NVIDIA RTX 3070 (8 ГБ VRAM);
− время обучения: ~4 часа.

Чекпоинты сохранялись после каждой эпохи. Активный чекпоинт — эпоха 14 (custom_8bit_v2.safetensors), выбранный по визуальной оценке качества спрайтов и динамике val_loss на отложенной выборке.

Для контроля обобщающей способности реализован скрипт research/train_lora.py, который использует разбивку train/val 70/30 (seed=42) и вычисляет val_loss после каждой эпохи. График train/val loss сохранён в research/loss_plot.png (рисунок 3.1).""",
    ),
    (
        "3.4 Архитектура веб-системы",
        """Система построена по клиент-серверной архитектуре (рисунок 3.2):

Клиент (браузер) → HTTP/JSON → Backend (FastAPI + SD Pipeline) → GPU

Компоненты репозитория:
− backend/main.py — API и inference;
− backend/lora/ — файлы LoRA-адаптеров;
− backend/history/ — сохранённые генерации;
− frontend/src/App.jsx — пользовательский интерфейс;
− frontend/dist/ — production-сборка, раздаётся FastAPI;
− dataset/ — pipeline подготовки данных;
− research/ — скрипты обучения и оценки.

Режимы развёртывания:
1) разработка: Vite dev server на порту 5173 с proxy на :8000;
2) production: npm run build, uvicorn раздаёт static + API на :8000;
3) публичный доступ: cloudflared tunnel через scripts/public-tunnel.bat.""",
    ),
    (
        "3.5 Реализация серверной части",
        """Серверная часть реализована в файле backend/main.py. При старте приложения выполняется загрузка StableDiffusionPipeline.from_pretrained("runwayml/stable-diffusion-v1-5") с переносом на CUDA при наличии GPU. Включён enable_attention_slicing() для экономии VRAM на 8 ГБ видеокартах.

Основные эндпоинты:
− POST /generate — генерация спрайта по промпту;
− GET /history — последние 20 генераций;
− GET /health — статус сервера и наличие LoRA-файлов.

Функция switch_lora(model_type) переключает LoRA-адаптер без перезагрузки базовой модели (листинг 3.1). При model_type="custom" загружается custom_8bit_v2.safetensors, при "public" — public_pixel_art.safetensors, при "base" — LoRA снимается.

Алгоритм генерации:
1) переключение LoRA;
2) формирование full_prompt с шаблоном стиля;
3) inference через pipe() с 25 шагами денойзинга (512×512);
4) масштабирование NEAREST до 80 или 128 px;
5) сохранение в history/ и возврат base64 PNG в JSON.""",
    ),
    (
        "3.6 Реализация клиентской части",
        """Клиентское приложение реализовано в frontend/src/App.jsx как single-page application на React 19.

Интерфейс разделён на три панели:
− левая — настройки (выбор модели, размер, число шагов, промпт);
− центральная — результат генерации и кнопка скачивания;
− правая — история последних генераций.

Пользователь выбирает одну из трёх моделей кнопками: «Базовая SD v1.5», «Публичная LoRA», «Обученная LoRA». Размер выходного изображения — 80×80 или 128×128 пикселей. Слайдер задаёт число шагов денойзинга от 10 до 50.

При нажатии «Сгенерировать» (или Ctrl+Enter) frontend отправляет POST /generate через axios. Полученное base64-изображение отображается в центральной панели. Кнопка «Скачать PNG» сохраняет файл локально.

При загрузке страницы выполняется GET /history для отображения thumbnails в правой панели. Клик по thumbnail восстанавливает изображение в центральной области (рисунок 3.3 — скриншот интерфейса добавляется при вёрстке).""",
    ),
]

CHAPTER_4: list[tuple[str, str]] = [
    (
        "4.1 Методика тестирования системы",
        """Основной акцент четвёртого раздела — проверка того, что разработанная веб-система работает корректно и решает поставленную задачу: пользователь может получить пиксельный спрайт по текстовому описанию.

Тестирование включало два направления:

1) Функциональное тестирование веб-приложения — проверка всех требований FR-1 – FR-6: ввод промпта, выбор параметров, генерация, скачивание, история, REST API.

2) Оценка качества генерации — визуальный анализ полученных спрайтов: чёткость пиксельных контуров, плоские цвета, белый фон, соответствие текстовому описанию.

Дополнительно, в исследовательских целях, проведено наглядное сравнение трёх режимов inference (base, public, custom) на фиксированном наборе промптов. Это не является главной целью работы, но показывает, зачем потребовалось дообучение LoRA.

Контроль обучения LoRA выполнялся по графику train/val loss (research/loss_log.csv, рисунок 3.1). Оборудование: NVIDIA RTX 3070, CUDA, fp16 inference.""",
    ),
    (
        "4.2 Дополнительное сравнение трёх режимов генерации",
        """В интерфейсе системы реализовано переключение между тремя режимами:
− Base SD v1.5 — базовая модель без LoRA;
− Public LoRA — публичный pixel-art адаптер (PixelArtRedmond);
− Custom LoRA — собственный адаптер, обученный на датасете проекта.

Сравнение проводилось визуально на одних и тех же промптах при steps=25 и output_size=128. Результаты обобщены в таблице 4.1.

Качественные наблюдения:
− промпт «warrior with red armor and sword» — base даёт painterly shading и размытые контуры; custom — плоские красные блоки брони и чёткий силуэт меча;
− промпт «green orc with axe» — custom ближе к стилю orc-спрайтов из обучающего датасета;
− промпт «skeleton mage with staff» — custom воспроизводит flat bones и фиолетовые акценты мантии.

Вывод дополнительного сравнения: дообученная LoRA даёт результат, наиболее близкий к целевому стилю проекта. Именно custom LoRA используется как основной режим генерации в разрабатываемой системе.

Зависимость времени генерации от числа шагов на RTX 3070 (таблица 4.2):
10 шагов — ~5 с, черновое качество;
25 шагов — ~12 с, рекомендуемый режим;
50 шагов — ~25 с, наилучшая детализация.""",
    ),
    (
        "4.3 Результаты функционального тестирования",
        """Тестирование проводилось вручную по матрице «требование — метод проверки»:

FR-1 (ввод промпта) — ввод текста в textarea, нажатие «Сгенерировать», получение изображения.
FR-2 (выбор модели) — переключение между тремя режимами, визуально различимые результаты.
FR-3 (steps, size) — изменение слайдера steps влияет на время и детализацию; кнопки 80/128 меняют размер.
FR-4 (скачивание PNG) — файл сохраняется локально и открывается в просмотрщике.
FR-5 (история) — после нескольких генераций thumbnails отображаются в правой панели.
FR-6 (REST API) — curl POST /generate возвращает JSON с полем image.

Проверка GET /health подтверждает доступность CUDA и наличие LoRA-файлов. Переключение LoRA base→custom→public занимает 1–2 секунды.

Все функциональные требования выполнены. Система позволяет выполнить полный сценарий «ввёл текст — получил спрайт» без использования командной строки — это и есть главный результат работы.""",
    ),
]

CONCLUSION = """
В ходе выполнения выпускной квалификационной работы разработана веб-система генерации 8-bit игровых персонажей на основе Stable Diffusion v1.5 и LoRA-адаптера, дообученного на собственном датасете из 507 пар «изображение — текстовое описание».

Кратко о том, что было сделано простыми словами: собраны и подписаны сотни примеров пиксельных спрайтов; на их основе обучена LoRA, «приучившая» нейросеть к нужному стилю; создан сайт, где любой человек может ввести описание персонажа и получить картинку за 10–15 секунд.

В первом разделе проведён анализ предметной области, рассмотрены аналоги (Aseprite, Automatic1111, облачные сервисы) и обоснован выбор Stable Diffusion v1.5 с методом LoRA. Сформулированы функциональные и нефункциональные требования к системе.

Во втором разделе описаны средства разработки: Python и FastAPI для серверной части, React и Vite для клиентской, PyTorch и diffusers для inference, kohya_ss для обучения LoRA.

В третьем разделе выполнены формирование датасета, предобработка изображений, обучение LoRA-адаптера и реализация веб-приложения с REST API, переключением LoRA и историей генераций.

В четвёртом разделе проведено функциональное тестирование — все требования выполнены. Дополнительно выполнено визуальное сравнение трёх режимов генерации, подтвердившее целесообразность собственного дообучения LoRA.

Поставленная цель — разработка веб-системы генерации 8-bit персонажей — достигнута. Разработанная система может использоваться независимыми разработчиками инди-игр для быстрого прототипирования персонажей.
"""

ABBREVIATIONS = """
В пояснительной записке применяются следующие сокращения и обозначения:

API — Application Programming Interface (программный интерфейс приложения)
GPU — Graphics Processing Unit (графический процессор)
HTTP — HyperText Transfer Protocol (протокол передачи гипертекста)
JSON — JavaScript Object Notation
LoRA — Low-Rank Adaptation
ML — Machine Learning (машинное обучение)
NES — Nintendo Entertainment System
PNG — Portable Network Graphics
REST — Representational State Transfer
RGB — Red Green Blue (цветовая модель)
SD — Stable Diffusion
UI — User Interface (пользовательский интерфейс)
VRAM — Video Random Access Memory
ВКР — выпускная квалификационная работа
ИИ — искусственный интеллект
ПЗ — пояснительная записка
СибГУТИ — Сибирский государственный университет телекоммуникаций и информатики
"""

BIBLIOGRAPHY = """
1. ГОСТ 7.32-2017. Отчёт о научно-исследовательской работе. Структура и правила оформления. — М.: Стандартинформ, 2017.
2. ГОСТ 2.105-2019. Единая система конструкторской документации. Общие требования к текстовым документам. — М.: Стандартинформ, 2019.
3. Ho J. et al. Denoising Diffusion Probabilistic Models // Advances in Neural Information Processing Systems. — 2020. — Vol. 33. — P. 6840–6851.
4. Rombach R. et al. High-Resolution Image Synthesis with Latent Diffusion Models // CVPR. — 2022. — P. 10684–10695.
5. Hu E. J. et al. LoRA: Low-Rank Adaptation of Large Language Models // ICLR. — 2022.
6. Radford A. et al. Learning Transferable Visual Models From Natural Language Supervision // ICML. — 2021. — P. 8748–8763.
7. [Электронный ресурс] Stable Diffusion v1.5. URL: https://huggingface.co/runwayml/stable-diffusion-v1-5 (дата обращения: 15.03.2026).
8. [Электронный ресурс] Hugging Face Diffusers. URL: https://huggingface.co/docs/diffusers (дата обращения: 15.03.2026).
9. [Электронный ресурс] Kohya ss — sd-scripts. URL: https://github.com/kohya-ss/sd-scripts (дата обращения: 15.03.2026).
10. [Электронный ресурс] FastAPI Documentation. URL: https://fastapi.tiangolo.com/ (дата обращения: 15.03.2026).
11. [Электронный ресурс] React Documentation. URL: https://react.dev/ (дата обращения: 15.03.2026).
12. [Электронный ресурс] PyTorch Documentation. URL: https://pytorch.org/docs/ (дата обращения: 15.03.2026).
13. [Электронный ресурс] OpenGameArt.org. URL: https://opengameart.org/ (дата обращения: 10.02.2026).
14. [Электронный ресурс] Kenney — Game Assets. URL: https://kenney.nl/ (дата обращения: 10.02.2026).
15. [Электронный ресурс] itch.io — Game assets. URL: https://itch.io/game-assets (дата обращения: 10.02.2026).
16. [Электронный ресурс] Aseprite. URL: https://www.aseprite.org/ (дата обращения: 01.02.2026).
17. [Электронный ресурс] Automatic1111 Stable Diffusion WebUI. URL: https://github.com/AUTOMATIC1111/stable-diffusion-webui (дата обращения: 01.02.2026).
18. [Электронный ресурс] CivitAI — Pixel Art LoRA. URL: https://civitai.com/ (дата обращения: 15.03.2026).
19. [Электронный ресурс] NVIDIA CUDA Toolkit. URL: https://docs.nvidia.com/cuda/ (дата обращения: 15.03.2026).
20. Sommerville I. Software Engineering. — 10th ed. — Pearson, 2016. — 794 p.
"""

APPENDIX_A = """Листинг серверной части (backend/main.py) — см. приложение А."""
APPENDIX_B = """Листинг клиентской части (frontend/src/App.jsx) — см. приложение Б."""
