# -*- coding: utf-8 -*-
"""Дополнительные подразделы — простой язык, объём 40–50 страниц."""

EXTRA_CH1 = [
    (
        "1.1.1 Что такое спрайт и пиксель-арт простыми словами",
        """Если открыть любую старую игру, например с консоли Dendy (NES), персонаж на экране — это и есть спрайт. По сути это маленькая картинка, которую игра «клеит» поверх фона и двигает по экрану. В те времена одного персонажа рисовали буквально из десятков цветных квадратиков — пикселей. Отсюда название «пиксель-арт»: искусство создавать узнаваемые образы из минимума деталей.

Почему этот стиль до сих пор популярен? Во-первых, он вызывает ностальгию. Во-вторых, такая графика производительна — старые телефоны и слабые компьютеры легко её отображают. В-третьих, ограниченная палитра цветов создаёт узнаваемый «игровой» вид, который сложно повторить фотореалистичной графикой.

Важно понимать разницу между «просто маленькой картинкой» и «настоящим пиксель-артом». Если уменьшить фотографию до 64×64 пикселей, получится нечитаемое пятно. Пиксель-арт рисуется специально: каждый пиксель выбирается вручную (или в данной работе — генерируется нейросетью с учётом правил стиля), чтобы силуэт оставался читаемым.""",
    ),
    (
        "1.1.2 Проблема создания спрайтов для инди-разработчиков",
        """Инди-разработчик — это человек или маленькая команда, которые делают игру самостоятельно, без бюджета крупной студии. По статистике платформы itch.io, большинство таких проектов используют 2D-графику, и значительная часть — именно пиксель-арт, потому что он доступнее 3D-моделирования.

Типичный цикл создания персонажа вручную выглядит так:
1) эскиз на бумаге или в редакторе;
2) прорисовка основных цветов;
3) добавление теней и бликов (в pixel art — одним-двумя оттенками);
4) обводка контура;
5) при необходимости — кадры анимации (ходьба, атака).

На пункты 2–4 у начинающего художника уходит от 4 до 8 часов. Если в игре 20 разных врагов — это уже сотни часов работы. Нейросеть не сокращает время до нуля, но позволяет за минуты получить десятки вариантов «на выбор», из которых можно взять лучший и доработать за 1–2 часа вместо 8.""",
    ),
    (
        "1.2.1 Подробный разбор аналога Aseprite",
        """Aseprite — профессиональный редактор для пиксель-графики. Его используют как любители, так и студии. Программа стоит около 20 долларов и работает на Windows, macOS и Linux.

Что умеет Aseprite:
− рисовать послойно (как в Photoshop, но для пикселей);
− создавать анимации с просмотром «луковичной» оболочки (onion skin) — видно предыдущий и следующий кадры;
− работать с ограниченной палитрой цветов;
− экспортировать sprite sheets — большие картинки, где все кадры анимации расположены в ряд.

Плюсы: полный контроль над каждым пикселем, industry standard.
Минусы: не генерирует картинки автоматически — только ручное рисование.

Разрабатываемая система не конкурирует с Aseprite, а дополняет его: нейросеть создаёт первый набросок, Aseprite — для доработки и анимации.""",
    ),
    (
        "1.2.2 Подробный разбор Automatic1111 WebUI",
        """Automatic1111 — бесплатная программа с графическим интерфейсом для Stable Diffusion. Её скачивают тысячи энтузиастов для генерации картинок на своём компьютере.

Плюсы:
− огромное количество настроек для опытных пользователей;
− поддержка LoRA, ControlNet, upscaling;
− активное сообщество и плагины.

Минусы для целевой аудитории данной работы:
− интерфейс перегружен — десятки полей и переключателей;
− нет готового режима «игровой спрайт»;
− чтобы сравнить три модели, нужно вручную менять настройки и записывать результаты;
− требует установки Python, Git, моделей — высокий порог входа.

Разрабатываемая система решает эту проблему: три кнопки выбора модели, одно поле для текста, одна кнопка «Сгенерировать». Время от открытия сайта до первой картинки — менее минуты.""",
    ),
    (
        "1.3.1 Как работает Stable Diffusion — объяснение без формул",
        """Stable Diffusion можно представить как очень сложный «фильтр», который умеет превращать текст в картинку. Процесс генерации похож на проявление фотографии, только в обратном порядке:

1) Нейросеть начинает с полного «шума» — случайных цветных пятен, как помехи на старом телевизоре.
2) За 25 шагов (это настраивается) она постепенно убирает шум, каждый раз чуть-чуть приближаясь к осмысленному изображению.
3) На каждом шаге она «смотрит» на ваш текстовый промпт и старается, чтобы картинка соответствовала описанию.
4) На выходе получается готовое изображение 512×512 пикселей.

Почему «из коробки» это не пиксель-арт? Потому что нейросеть обучалась на миллионах обычных фотографий и рисунков из интернета. Она «привыкла» рисовать с плавными переходами цветов и реалистичным светом. Чтобы научить её рисовать «квадратиками», нужно показать ей сотни примеров именно пиксельных спрайтов — это и делает этап обучения LoRA.""",
    ),
    (
        "1.3.2 Что такое LoRA и зачем она нужна",
        """LoRA (Low-Rank Adaptation) — способ «дообучить» большую готовую нейросеть, не меняя её целиком. Аналогия: представьте опытного художника, который умеет рисовать всё подряд. LoRA — это как короткий курс «рисуй только в стиле NES», после которого художник сохраняет все свои навыки, но предпочитает плоские цвета и пиксельные контуры.

Технически LoRA — это небольшой файл (~36 МБ), который подключается к основной модели (~4 ГБ). При генерации картинки учитываются и базовые знания нейросети («это воин», «это меч»), и стилевые предпочтения LoRA («рисуй плоскими цветами, белый фон»).

В работе используются три режима:
− Base — только базовая модель, без LoRA (для сравнения);
− Public — чужая LoRA с сайта CivitAI, обученная на общем pixel art;
− Custom — собственная LoRA, обученная на датасете из 507 спрайтов проекта.""",
    ),
    (
        "1.4.1 Пояснение к требованиям простым языком",
        """Требования FR-1 – FR-6 из раздела 1.4 означают следующее для обычного пользователя:

FR-1 — вы просто печатаете описание в поле на сайте, как в поисковой строке.
FR-2 — три кнопки позволяют переключить «режим рисования» и сравнить результаты.
FR-3 — ползунок «шагов» влияет на качество и время: меньше шагов — быстрее, но грубее; больше — дольше, но детальнее.
FR-4 — готовую картинку можно скачать одним кликом.
FR-5 — справа видны последние 20 ваших генераций — удобно вернуться к понравившемуся варианту.
FR-6 — продвинутые пользователи могут обращаться к серверу напрямую через программные команды (API), без браузера.""",
    ),
]

EXTRA_CH2 = [
    (
        "2.0 Основные понятия для читателя",
        """Перед описанием инструментов разработки полезно договориться о терминах, которые будут встречаться по всей работе.

Нейросеть — программа, которая находит закономерности в большом количестве примеров. В данной работе: «на этих 507 картинках — пиксельные персонажи, значит новые тоже надо рисовать так».

GPU (видеокарта) — устройство в компьютере, которое ускоряет вычисления нейросети в десятки раз по сравнению с обычным процессором. Без GPU генерация одной картинки занимала бы минуты вместо секунд.

Промпт — текстовое описание желаемой картинки. Чем конкретнее промпт («red armored knight with silver sword»), тем точнее результат.

Inference (вывод) — процесс генерации картинки уже обученной моделью. Обучение делается один раз; inference — каждый раз, когда пользователь нажимает «Сгенерировать».

Backend (серверная часть) — программа, которая работает на компьютере с видеокартой и выполняет генерацию. Frontend (клиентская часть) — то, что отображается в браузере пользователя.

REST API — способ общения между браузером и сервером через стандартные HTTP-запросы. Браузер «просит» сервер: «нарисуй вот это» — сервер «отвечает» картинкой.""",
    ),
    (
        "2.1.1 Клиент-серверная архитектура простыми словами",
        """Разрабатываемая система построена по схеме «клиент — сервер», как большинство современных сайтов.

Сервер — это программа на компьютере с видеокартой. Она хранит нейросеть в памяти и выполняет тяжёлые вычисления. Клиент — это вкладка браузера у пользователя. Она показывает форму ввода, кнопки и картинку, но сама нейросеть не содержит.

Почему так? Во-первых, видеокарта нужна только на одном компьютере (сервере), а пользователи могут заходить даже с телефона. Во-вторых, обновление модели делается в одном месте — не нужно переустанавливать программу у каждого пользователя. В-третьих, это стандартный подход для веб-приложений — тот же принцип, что у онлайн-магазинов или социальных сетей.

Обмен данными происходит в формате JSON — текстовый формат, понятный и человеку, и программе. Картинка передаётся в закодированном виде (base64), чтобы её можно было вставить прямо в HTML-страницу без отдельного файла.""",
    ),
    (
        "2.1.2 Библиотека Axios",
        """Axios — небольшая JavaScript-библиотека для отправки HTTP-запросов из браузера на сервер. Когда пользователь нажимает «Сгенерировать», именно axios отправляет POST-запрос с текстом промпта и настройками.

Плюсы:
− простой синтаксис;
− автоматическое преобразование данных в JSON;
− удобная обработка ошибок (если сервер недоступен или LoRA не найдена — пользователь видит понятное сообщение).

Минусы:
− дополнительная зависимость в проекте (хотя она очень маленькая).

Альтернатива — встроенный в браузер fetch API. Axios выбран за более удобную обработку ошибок FastAPI.""",
    ),
    (
        "2.3.1 Что такое diffusers и pipeline",
        """Библиотека diffusers — это «обёртка» вокруг Stable Diffusion, которая скрывает сложность. Вместо того чтобы вручную загружать три отдельных компонента (текстовый энкодер, U-Net, VAE), вызывать их по очереди и склеивать результат, разработчик пишет:

pipe = StableDiffusionPipeline.from_pretrained("runwayml/stable-diffusion-v1-5")
result = pipe("pixel art warrior")

StableDiffusionPipeline — это pipeline (конвейер): готовая цепочка шагов от текста до картинки. Методы load_lora_weights и unload_lora_weights позволяют подключать и отключать LoRA без перезагрузки всей модели — это ключевая функция для переключения трёх режимов в веб-интерфейсе.""",
    ),
    (
        "2.5.1 Три части Stable Diffusion — простое объяснение",
        """Stable Diffusion внутри состоит из трёх «блоков», каждый выполняет свою роль:

1) CLIP Text Encoder — «переводчик» с человеческого языка на язык нейросети. Вы пишете «red warrior», он превращает это в набор чисел (embeddings), которые описывают смысл фразы.

2) U-Net — «художник», который рисует. Он 25 раз подряд убирает шум с картинки, каждый раз сверяясь с «переводом» от CLIP. Именно к U-Net подключается LoRA — «художник» начинает рисовать в новом стиле.

3) VAE — «кодировщик изображений». Нейросеть работает не с полной картинкой 512×512 (это медленно), а со сжатым представлением 64×64. VAE сжимает и распаковывает — как ZIP для картинок.

Пользователю не нужно взаимодействовать с этими блоками напрямую — всё происходит автоматически внутри pipeline.""",
    ),
    (
        "2.5.2 Текстовый энкодер CLIP — зачем он нужен",
        """CLIP Text Encoder — часть Stable Diffusion, которая «понимает» текст промпта. Когда пользователь пишет «red warrior», CLIP превращает эти слова в числовое представление, которое U-Net использует при рисовании.

Важно: CLIP в данной работе — компонент архитектуры модели, а не метрика оценки качества. Качество генерации оценивалось визуально — по тому, насколько результат похож на пиксельный спрайт и соответствует описанию.""",
    ),
]

EXTRA_CH3 = [
    (
        "3.1.1 Откуда взяты изображения и почему это легально",
        """Все 507 изображений скачаны с сайтов, где авторы asset packs распространяют их под открытыми лицензиями:

− Kenney.nl — лицензия CC0 (общественное достояние, можно использовать свободно);
− OpenGameArt.org — смешанные лицензии, отбирались только паки с разрешением на использование;
− itch.io — бесплатные паки с указанием лицензии в описании.

Перед включением в датасет проверялось, что лицензия допускает использование в учебных и некommercial проектах. Это важно с этической точки зрения: нейросеть обучается на чужих работах, поэтому необходимо уважать права авторов исходных спрайтов.""",
    ),
    (
        "3.2.1 Пошаговый пример предобработки одного файла",
        """Рассмотрим на конкретном примере, что происходит с одним спрайтом до попадания в датасет.

Исходный файл: raw/orc_warrior.png, размер 96×128 пикселей, формат RGBA (с прозрачным фоном).

Шаг 1. Открытие файла программой Pillow (библиотека Python для работы с картинками).

Шаг 2. Обрезка до квадрата 96×96 — берётся центральная часть, лишнее сверху и снизу отсекается. Квадрат нужен, потому что Stable Diffusion работает с квадратными изображениями.

Шаг 3. Замена прозрачного фона на белый. Прозрачность (альфа-канал) нейросеть обрабатывает хуже, чем сплошной белый цвет.

Шаг 4. Увеличение до 512×512 методом NEAREST. «Ближайший сосед» означает: каждый новый пиксель копирует цвет ближайшего старого, без смешивания. Если бы использовалась обычная интерполяция, края стали бы размытыми.

Шаг 5. Сохранение как 0332_orc.png в папку images/. Параллельно создаётся файл 0332_orc.txt с текстовым описанием на английском языке.""",
    ),
    (
        "3.2.2 Зачем нужны текстовые описания к каждой картинке",
        """К каждому из 507 изображений прилагается .txt файл с описанием — caption. Это необходимо для обучения: нейросеть учится связывать картинку с текстом. Если показывать ей только картинки без слов, она выучит стиль, но не научится «понимать» промпты пользователя.

Все описания составлены по единому шаблону:
[стиль] + [кто это] + [цвета и детали] + [фон и теги]

Пример: «8-bit pixel art character sprite, green orc warrior with brown leather armor, holding iron axe, NES style, retro game, simple flat colors, white background».

Единообразие важно: если в одних описаниях написано «pixel art», а в других «retro sprite», нейросеть получает путаный сигнал. Шаблон это предотвращает.""",
    ),
    (
        "3.3.1 Что такое эпоха, rank и learning rate",
        """При описании обучения LoRA встречаются термины, которые стоит пояснить:

Эпоха — один полный «просмотр» всего датасета. 507 картинок × 5 повторов = 2535 шагов за эпоху. Всего 15 эпох — модель 15 раз «перечитала» все примеры.

Rank (network_dim=32) — «ёмкость» LoRA. Меньше rank — проще адаптер, но может не хватить для сложного стиля. Больше — риск переобучения на маленьком датасете. 32 — рекомендуемое значение для style LoRA на 300–500 картинках.

Learning rate (1×10⁻⁴) — «скорость обучения». Слишком большая — модель «перепрыгивает» через правильные ответы. Слишком маленькая — обучение идёт неделями. 0.0001 — стандартное значение для LoRA.

AdamW8bit — алгоритм, который обновляет веса LoRA на каждом шаге. «8bit» означает экономию видеопамяти — важно для видеокарты с 8 ГБ.""",
    ),
    (
        "3.3.2 Выбор оптимального чекпоинта",
        """После каждой эпохи обучения сохранялся отдельный файл LoRA (чекпоинт). Выбор лучшего — не всегда «последний»:

− Эпохи 1–5: модель ещё «не поняла» стиль — размытые контуры, серый фон.
− Эпохи 6–12: стиль улучшается, появляются плоские цвета.
− Эпохи 13–15: лучший стиль, но иногда цвета слишком яркие (переобучение).

Эпоха 14 выбрана как компромисс: визуально чистые спрайты без излишней насыщенности цветов, val_loss на приемлемом уровне. Последняя эпоха не всегда лучшая — поэтому все чекпоинты сохранялись и сравнивались вручную.""",
    ),
    (
        "3.3.3 График обучения — как его читать",
        """На рисунке 3.1 (research/loss_plot.png) показаны две линии:
− train_loss — ошибка на обучающих данных (то, на чём модель училась);
− val_loss — ошибка на проверочных данных (то, чего модель «не видела»).

Если train_loss падает, а val_loss растёт — это переобучение: модель «зазубрила» примеры, но хуже работает на новых. В данной работе val_loss остаётся стабильным до 15-й эпохи — обучение прошло корректно.

Читателю достаточно понимать: чем ниже val_loss, тем лучше модель обобщает. Но окончательный выбор всё равно делается глазами — смотреть на реальные сгенерированные спрайты.""",
    ),
    (
        "3.4.1 Схема работы системы для неспециалиста",
        """На рисунке 3.2 рекомендуется изобразить следующую схему (описание для вёрстки):

[Пользователь в браузере] → стрелка «вводит текст, нажимает кнопку» → [Сервер FastAPI] → стрелка «запускает нейросеть» → [Видеокарта GPU] → стрелка «возвращает картинку» → [Сервер] → стрелка «отправляет в браузер» → [Пользователь видит спрайт]

Под капотом сервер при каждом запросе:
1) проверяет, какая LoRA нужна, и переключает её (1–2 сек);
2) формирует полный промпт с добавлением «pixel art, 8-bit, white background»;
3) запускает 25 итераций денойзинга (~12 сек);
4) уменьшает картинку до 80 или 128 px;
5) сохраняет копию в history/ и отправляет пользователю.""",
    ),
    (
        "3.5.1 Описание работы сервера по шагам",
        """Файл backend/main.py — «сердце» системы. Рассмотрим, что происходит при запуске и при запросе пользователя.

При запуске (один раз):
− загружается Stable Diffusion v1.5 (~4 ГБ в память видеокарты);
− включается attention_slicing — экономия памяти ценой ~10% скорости;
− создаются папки lora/ и history/, если их нет.

При каждом POST /generate:
1) switch_lora(model_type) — если пользователь выбрал другую модель с прошлого раза, старая LoRA выгружается, новая загружается;
2) full_prompt = «pixel art sprite, 8-bit character, {промпт пользователя}, NES style…»;
3) pipe(...) — нейросеть генерирует 512×512;
4) resize NEAREST до output_size;
5) save в history/, return JSON с base64.

Код функции switch_lora приведён в листинге А.1 (приложение А).""",
    ),
    (
        "3.5.2 REST API — примеры для воспроизведения",
        """Любой человек с доступом к серверу может проверить работу API через командную строку (терминал):

Проверка статуса:
curl http://127.0.0.1:8000/health

Генерация спрайта:
curl -X POST http://127.0.0.1:8000/generate -H "Content-Type: application/json" -d '{"prompt":"blue knight with shield","model_type":"custom","steps":25,"output_size":128}'

Ответ — JSON с полем image (длинная строка base64). Это тот же процесс, что происходит при нажатии кнопки в браузере, только без графического интерфейса.

Документация всех эндпоинтов доступна в браузере по адресу http://127.0.0.1:8000/docs — интерактивная страница Swagger, где можно отправить тестовый запрос.""",
    ),
    (
        "3.6.1 Подробное описание интерфейса",
        """Интерфейс (рисунок 3.3 — скриншот добавляется при вёрстке) намеренно сделан простым. Ниже — описание каждого элемента.

Левая панель «Настройки»:
− три кнопки модели с подписями на русском языке;
− две кнопки размера: 80×80 (более «ретро») и 128×128 (больше деталей);
− ползунок «Шагов генерации» от 10 до 50;
− поле «Описание персонажа» — основной ввод пользователя;
− поле «Негативный промпт» — что НЕ должно быть на картинке (по умолчанию: realistic, blurry, 3d…);
− кнопка «Сгенерировать» (или Ctrl+Enter).

Центральная панель:
− область предпросмотра — здесь появляется результат;
− подпись с полным промптом, который реально отправила нейросеть;
− кнопка «Скачать PNG».

Правая панель «История»:
− до 20 последних генераций в виде миниатюр;
− клик по миниатюре восстанавливает картинку в центре;
− под каждой — подпись модели и размера.""",
    ),
    (
        "3.6.2 Как собрать и запустить проект",
        """Инструкция для воспроизведения результатов (также есть в README проекта):

1) Установить Python 3.10+, Node.js 18+, драйвер NVIDIA CUDA.
2) pip install -r backend/requirements.txt
3) cd frontend && npm install && npm run build
4) uvicorn backend.main:app --host 0.0.0.0 --port 8000
5) Открыть http://127.0.0.1:8000 в браузере.
6) Убедиться, что в backend/lora/ лежит файл custom_8bit_v2.safetensors.

При первом запуске модель Stable Diffusion скачается автоматически с HuggingFace (~4 ГБ). Последующие запуски используют кэш и загружаются быстрее.""",
    ),
]

EXTRA_CH4 = [
    (
        "4.1.1 Критерии оценки качества генерации",
        """Поскольку главная цель работы — разработка веб-системы, а не сравнение моделей, качество оценивалось по понятным визуальным критериям:

1) Пиксельная чёткость — границы без размытия, видны отдельные «квадратики».
2) Плоские цвета — нет плавных градиентов и реалистичных теней.
3) Белый фон — однородный, без пятен и артефактов.
4) Семантическое соответствие — на картинке тот персонаж, который описан в промпте (воин, орк, скелет и т.д.).
5) Пригодность для прототипа — результат можно использовать как основу для доработки в редакторе.

Дополнительно проводилось сравнение трёх режимов inference на одних промптах — только для демонстрации эффекта LoRA, не как самостоятельная research-задача.""",
    ),
    (
        "4.2.1 Качественный анализ — что видит глаз",
        """При дополнительном визуальном сравнении трёх режимов на одном промпте «warrior with red armor and sword» наблюдается следующее:

Base SD v1.5 рисует фигуру, похожую на иллюстрацию из книги: мягкие тени, плавные переходы, фон может быть серым или с градиентом. Это красиво, но не спрайт.

Public LoRA добавляет «ступеньки» на краях — уже похоже на pixel art. Однако пропорции могут быть «мультяшными», не совпадающими со стилем конкретного проекта.

Custom LoRA выдаёт плоские красные блоки брони, чёткий силуэт меча, чистый белый фон — визуально близко к спрайтам из обучающего датасета.

Рекомендация для пользователя: для повседневной работы использовать режим «Обученная LoRA»; переключение на другие режимы — по желанию, чтобы увидеть разницу.""",
    ),
    (
        "4.2.2 Типичные ошибки генерации и как их избежать",
        """Даже лучшая модель иногда ошибается. Типичные проблемы:

1) Лишняя рука или искривлённое оружие — уменьшить или увеличить steps, попробовать другой промпт.
2) Не тот цвет брони — сделать промпт конкретнее: «bright red armor» вместо «colorful armor».
3) Серый или грязный фон — проверить, что выбрана LoRA (base часто даёт не белый фон).
4) Размытые пиксели — убедиться, что output_size 128, не 80; не масштабировать результат в Photoshop с сглаживанием.
5) Два персонажа вместо одного — добавить в промпт «single character», в негативный — «multiple people».

Большинство проблем решается повторной генерацией: каждый запуск даёт новый случайный результат, и из 5–10 попыток обычно находится приемлемый вариант.""",
    ),
    (
        "4.3.1 Результаты ручного тестирования",
        """Каждое функциональное требование проверялось вручную — так же, как это сделал бы обычный пользователь:

Тест 1. Ввод промпта «skeleton archer» → нажатие «Сгенерировать» → через ~12 секунд появился спрайт скелета. Пройден.

Тест 2. Переключение base → public → custom на одном промпте → три визуально разных результата. Пройден.

Тест 3. Steps=10 → ~5 сек, качество ниже; steps=50 → ~25 сек, детальнее. Пройден.

Тест 4. «Скачать PNG» → файл открывается в стандартном просмотрщике. Пройден.

Тест 5. Три генерации подряд → три миниатюры в истории справа. Пройден.

Тест 6. curl POST /generate → JSON с полем image. Пройден.

Все требования FR-1 – FR-6 выполнены.""",
    ),
    (
        "4.3.2 Ограничения системы — честно о минусах",
        """Важно понимать, чего система НЕ делает:

− не создаёт анимацию (только один кадр);
− не гарантирует идеальный результат с первой попытки;
− не понимает русский промпт так же хорошо, как английский;
− не удаляет белый фон автоматически (нужна ручная обработка в Aseprite);
− не работает быстро без видеокарты NVIDIA (на CPU — 3–8 минут на картинку);
− не рассчитана на одновременную работу сотен пользователей без доработки.

Эти ограничения не отменяют практической пользы для прототипирования, но их следует иметь в виду при планировании production-проекта.""",
    ),
]

VOLUME_CH1 = [
    (
        "1.5 Общий принцип работы системы",
        """Прежде чем углубляться в технические детали последующих разделов, полезно увидеть «большую картину». Разрабатываемая система решает одну задачу: текст → картинка спрайта. Весь проект можно представить как конвейер из пяти этапов:

Этап 1. Подготовка (делается один раз автором). Собрать сотни примеров пиксельных спрайтов, подписать их текстом, обучить LoRA — «научить нейросеть рисовать в нужном стиле».

Этап 2. Развёртывание (делается один раз на сервере). Установить программы, загрузить базовую модель и файлы LoRA, запустить сервер.

Этап 3. Использование (каждый раз пользователем). Открыть сайт, ввести описание, нажать кнопку.

Этап 4. Генерация (автоматически, ~12 секунд). Сервер формирует промпт, запускает нейросеть, уменьшает картинку.

Этап 5. Результат. Пользователь скачивает PNG, при необходимости дорабатывает в редакторе, импортирует в игру.

Читатель, который усвоил эту схему, уже понимает 80% работы. Дальнейшие разделы лишь детализируют каждый этап.""",
    ),
]

VOLUME_CH2 = [
    (
        "2.6 Словарь терминов работы",
        """Датасет — набор пар «картинка + описание», на котором обучается LoRA. В проекте: 507 пар.

Inference — генерация новой картинки обученной моделью (в отличие от обучения).

Fine-tuning — дообучение готовой модели на новых данных. LoRA — один из способов fine-tuning.

Safetensors — формат файла для хранения весов нейросети (безопаснее старого pickle).

Uvicorn — программа, которая «поднимает» сервер FastAPI и слушает HTTP-запросы.

Vite — инструмент, который собирает React-код в файлы, понятные браузеру.

Base64 — способ записать бинарную картинку текстом для передачи в JSON.

Negative prompt — описание того, чего не должно быть на картинке (realistic, blurry…).

Attention slicing — приём экономии видеопамяти: нейросеть обрабатывает картинку частями, а не целиком сразу.""",
    ),
]

VOLUME_CH3 = [
    (
        "3.7 Рекомендации по составлению промптов",
        """Качество результата сильно зависит от того, как пользователь формулирует описание. Рекомендации:

1) Пишите на английском — нейросеть лучше понимает этот язык.
2) Один персонаж на промпт — не «two warriors fighting», а «single warrior».
3) Конкретные цвета — «red armor», «blue robe», не «colorful clothes».
4) Указывайте оружие и тип — «skeleton mage with wooden staff», не просто «magic creature».
5) Не противоречьте стилю — не пишите «photorealistic pixel art».
6) Не очищайте негативный промпт без причины — он отсекает реализм и размытие.

Система сама добавляет «pixel art, 8-bit, NES style, white background» — пользователю достаточно описать самого персонажа.""",
    ),
    (
        "3.8 Импорт спрайта в игровой движок",
        """После скачивания PNG его нужно подготовить для игры:

Unity 2D:
1) Import PNG в проект.
2) Inspector → Texture Type = Sprite.
3) Filter Mode = Point (no filter) — критично для pixel art!
4) Перетащить на Sprite Renderer в сцене.
5) Удалить белый фон: в Aseprite или через shader в Unity.

Godot 4:
1) Import PNG.
2) Project Settings → Rendering → Textures → Default Texture Filter = Nearest.
2) Добавить Sprite2D node, назначить текстуру.
3) Прозрачность — через Aseprite или ColorKey shader.

Без Filter Mode = Point / Nearest движок размоет пиксели при масштабировании — спрайт потеряет «пиксельный» вид.""",
    ),
]

VOLUME_CH4 = [
    (
        "4.4 Сводная таблица: что было сделано",
        """Задача 1 (анализ предметной области) — выполнена в разделе 1: описаны спрайты, аналоги, выбор SD+LoRA, требования.

Задача 2 (средства разработки) — выполнена в разделе 2: Python, FastAPI, React, PyTorch, diffusers, kohya_ss.

Задача 3 (датасет и обучение) — выполнена в разделе 3.1–3.3: 507 пар, prepare_dataset.py, custom_8bit_v2.safetensors.

Задача 4 (веб-приложение) — выполнена в разделе 3.4–3.6: FastAPI + React, три режима, история.

Задача 5 (тестирование) — выполнена в разделе 4: функциональное тестирование, визуальная оценка качества.

Критерий успеха: веб-система генерирует пиксельные спрайты, все FR-1 – FR-6 выполнены, LoRA обучена на 507 парах.""",
    ),
    (
        "4.5 Рекомендации пользователям и разработчикам",
        """Для начинающего gamedev-разработчика:
− начните с модели «Обученная LoRA» и steps=25;
− генерируйте 5–10 вариантов одного персонажа, выберите лучший;
− доработайте в Aseprite: прозрачность, палитра, контур;
− не ждите готовой анимации — только статичный кадр.

Для продолжения разработки системы:
− добавить очередь запросов для нескольких пользователей;
− поддержка русских промптов через дообучение text encoder;
− генерация sprite sheet (несколько кадров);
− встроенная палитризация (ограничение до 16–32 цветов);
− авторизация при публичном доступе через tunnel.""",
    ),
]

CH1_APPENDIX_TEXT = """
Дополнительно стоит отметить экономический аспект. Нанять pixel artist на фрилансе для одного персонажа — от 2000 до 8000 рублей (4–8 часов работы). Покупка asset pack на itch.io — 500–3000 рублей за набор, но без гарантии уникальности. Генерация через собственную систему после однократной настройки (~4 часа обучения LoRA) обходится практически бесплатно: только электричество и время ожидания 12 секунд на попытку. Для студента или solo-разработчика это существенное преимущество на этапе прототипирования.
"""

CH2_APPENDIX_TEXT = """
Все версии библиотек зафиксированы в backend/requirements.txt и frontend/package.json для воспроизводимости. При повторении эксперimentа через год рекомендуется использовать те же версии: torch>=2.3, diffusers>=0.27, transformers>=4.40, react 19, vite 8. Обновление major-версий может потребовать правок в коде из-за изменений API.
"""

CH3_APPENDIX_TEXT = """
Полный перечень каталогов проекта для ориентирования читателя:
backend/ — сервер и LoRA-файлы;
frontend/ — интерфейс;
dataset/raw/ — исходные спрайты;
dataset/images/ — подготовленный датасет 507 пар;
research/ — скрипты обучения, оценки, графики;
scripts/ — вспомогательные скрипты (tunnel).
Подробные команды запуска — в README.md в корне репозитория.
"""

CH4_APPENDIX_TEXT = """
При оформлении итогового PDF рекомендуется включить рисунки:
Рисунок 1.1 — пример спрайта из датасета;
Рисунок 3.1 — график train/val loss (loss_plot.png);
Рисунок 3.2 — схема клиент-сервер;
Рисунок 3.3 — скриншот веб-интерфейса;
Рисунок 4.1 — сравнение base / public / custom на одном промпте (три картинки рядом).
Эти иллюстрации делают работу понятной комиссии без чтения всего текста.
"""

TUTORIAL_BLOCKS = ""
EXTENDED_NARRATIVE = ""
